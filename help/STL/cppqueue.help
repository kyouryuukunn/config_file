?? TITLE : C++ キュー

cppreference.com -> C++ キュー

C++ キュー(std::queue)

キューはプログラマにFIFO(ファースト・イン、ファースト・アウト)のデータ構造を提供するコンテナアダプタである。

back()  最後の要素を返す
empty()  キューが空の時にtrueを返す
front()  最初の要素を返す
pop()  最初の要素を削除する
push()  キューの最後に要素を追加する
size()  キューの要素数を返す


-------------------------------------------------------------------------------------- 



?? TITLE : C++ キュー

cppreference.com -> C++ キュー -> 詳細

C++ キュー(std::queue)

-------------------------------------------------------------------------------

back

文法:



   TYPE &back();


現在のキューの最後の要素への参照を返す。

-------------------------------------------------------------------------------

empty

文法:



  bool empty();


現在のキューが空ならtrueを返す。空でない場合にはfalseを返す。

-------------------------------------------------------------------------------

front

文法:



   TYPE &front();


現在のキューの先頭の要素への参照を返す。

-------------------------------------------------------------------------------

pop

文法:



  void pop();


現在のキューの先頭の要素を破棄する。

-------------------------------------------------------------------------------

push

文法:



  void push( const TYPE &値 );


現在のキューに値 を追加する。

-------------------------------------------------------------------------------

size

文法:



  size_type size();


現在のキューの要素数を返す。

