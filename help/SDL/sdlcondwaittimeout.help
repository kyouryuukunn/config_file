TITLE : SDL_CondWaitTimeout

SDL Library Documentation
PrevNext

SDL_CondWaitTimeout

Name

SDL_CondWaitTimeout -- 条件変数を制限時間つきで待ちます。

Synopsis

#include "SDL.h" #include "SDL_thread.h"

int SDL_CondWaitTimeout(SDL_cond *cond, SDL_mutex *mutex, Uint32 ms);

Description


条件変数condを最大msミリセカンドまで待ちます。mutex mutは呼び出しの 前にロックされていなければなりません。 指定された時間までにシグナルを受け取らなかったときはSDL_MUTEX_TIMEDOUTを、時間内にシグナルを受け取ったときは0を、 エラーが起こると-1を、それぞれ返します。

See Also

SDL_CondWait

PrevHomeNext
SDL_CondWaitUpTime
