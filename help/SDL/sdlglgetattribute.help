TITLE : SDL_GL_GetAttribute

SDL Library Documentation
PrevNext

SDL_GL_GetAttribute

Name

SDL_GL_GetAttribute --  特殊な SDL/OpenGL 属性の値を取得します。

Synopsis

#include "SDL.h"

int SDL_GL_GetAttribute(SDLGLattr attr, int *value);

Description


SDL/OpenGL 属性 attr の値を value に置きます。 これは、 SDL_SetVideoMode の呼び出しの後に、 あなたの指定した属性が期待通りに セットされたかどうかを チェックするのに有用です。

Return Value


成功の場合は 0 を、 失敗の場合は -1 を返します。

See Also

SDL_GL_SetAttribute, GL Attributes

PrevHomeNext
SDL_GL_GetProcAddressUpSDL_GL_SetAttribute
