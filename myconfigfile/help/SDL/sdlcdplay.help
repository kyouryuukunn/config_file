TITLE : SDL_CDPlay

SDL Library Documentation
PrevNext

SDL_CDPlay

Name

SDL_CDPlay --  CD を再生します。

Synopsis

#include "SDL.h"

int SDL_CDPlay(SDL_CD *cdrom, int start, int length);

Description


指定された cdrom を、 フレーム startから length フレームだけ 再生します。

Return Values


成功の時は 0 を返し、 エラーの時は -1 を返します。

See Also

SDL_CDPlayTracks, SDL_CDStop

PrevHomeNext
SDL_CDStatusUpSDL_CDPlayTracks
