TITLE :         BohYoh.com【Ｃ言語講座】標準ライブラリ pow    

　pow
Ｃ言語 標準ライブラリ   アルファベット順索引   ヘッダ別索引   ホームページへ　Ｃ言語講座のページ

ベき乗関数
pow
ヘッダ  #include <math.h>
形　式  double pow(double x, double y);
機　能  pow関数は、xのy乗を計算する。xが負でyが整数値でない場合、定義城エラーが発生する。xが0かつyが0以下で結果が表現できない場合、定義域エラーが発生する。値域エラーが発生することがある。
返却値  pow関数は、xのy乗の値を返す。

-------------------------------------------------------------------------------
