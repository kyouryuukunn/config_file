TITLE :         BohYoh.com【Ｃ言語講座】標準ライブラリ srand    

　srand
Ｃ言語 標準ライブラリ   アルファベット順索引   ヘッダ別索引   ホームページへ　Ｃ言語講座のページ

擬似乱数列生成関数
srand
ヘッダ  #include <stdlib.h>
形　式  void srand(unsigned int seed);
機　能  srand関数は、後続するrandの呼出しで返す新しい擬似乱数列の種（たね）をseedに設定する。srand関数を同じ種の値で呼び出すと、同じ擬似乱数列が生成される。rand関数をsrand関数より前に呼び出した場合、srand関数が最初に種の値を1として呼ばれたときと同じ列が生成される。処理系は、いかなるライブラリ関数もsrand関数を呼び出さないかのように動作しなければならない。
返却値  srand関数は、値を返さない。

-------------------------------------------------------------------------------
