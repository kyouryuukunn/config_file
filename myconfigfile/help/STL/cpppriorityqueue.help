?? TITLE : C++ 優先順位付きキュー(std::priority_queue)

cppreference.com -> C++ 優先順位付きキュー

C++ 優先順位付きキュー(std::priority_queue)

C++の優先順位付きキューはキューと似ているが、 データ構造の中の要素は、与えられた比較方法を使用して順番に並べられる。

empty()  優先順位付きキューが空ならtrueを返す
pop()  最初の要素を削除する
push()  要素を追加する
size()  優先順位付きキューに格納されている要素数を返す
top()  優先順位の高い要素を返す


-------------------------------------------------------------------------------------- 



?? TITLE : C++ 優先順位付きキュー

cppreference.com -> C++ 優先順位付きキュー -> 詳細

C++ 優先順位付きキュー(std::priority_queue)

-------------------------------------------------------------------------------

empty

文法:



  bool empty();


現在の優先順位付きキューが空ならtrueを返す。空でない場合にはfalseを返す。

-------------------------------------------------------------------------------

pop

文法:



  void pop();


現在の優先順位付きキューの先頭の要素を破棄する。

-------------------------------------------------------------------------------

push

文法:



  void push( const TYPE &値 );


現在の優先順位付きキューに値 を追加する。

-------------------------------------------------------------------------------

size

文法:



  size_type size();


現在のキューの要素数を返す。

-------------------------------------------------------------------------------

top

文法:



   TYPE &top();


優先順位が一番高い要素への参照を返す。 要素はpop()を使うことでしか削除できない。

